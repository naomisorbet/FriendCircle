<h1><%= @friend_circle.name%></h1>

<ul>
	<% @friend_circle.members.each do |member| %>
	<li><%= member.username %></li>
	<%end%>
</ul>

<%= button_to "Delete Circle",
		user_friend_circle_url(current_user, @friend_circle), :method => :delete %>